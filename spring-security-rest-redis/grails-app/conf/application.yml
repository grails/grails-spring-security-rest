grails:
    profile: plugin
    codegen:
        defaultPackage: spring.security.rest.redis
    spring:
        transactionManagement: false
info:
    app:
        name: '@info.app.name@'
        version: '@info.app.version@'
        grailsVersion: '@info.app.grailsVersion@'
spring:

    groovy:
        template:
            check-template-location: false
