language: groovy
jdk:
  - oraclejdk7
branches:
  only:
    - master
services: memcached
script: ./grailsw test-app
  && cd test/apps
  && for app in `ls .`; do cd $app && ../../../grailsw test-app ; done
  && ./doc.sh
